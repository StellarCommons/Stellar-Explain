# Changesets

This directory contains changeset files that describe changes to `@stellar-explain/cli`.

When a PR includes changes that affect the public API or behavior of the CLI,
a changeset file should be added. Run `npm run changeset` from the
`packages/cli` directory to create one.

See [CONTRIBUTING.md](../../CONTRIBUTING.md) for the full workflow.
